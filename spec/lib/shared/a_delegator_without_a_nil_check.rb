shared_examples 'a delegator without a nil check' do
  # it { should     delegate(method_name).to(receiver).allow_nil(false)  }
  # it { should_not delegate(method_name).to(receiver).allow_nil(true)  }
  # it { should_not delegate(method_name).to(receiver).allow_nil  }
end
